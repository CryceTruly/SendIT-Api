#language to use in tavis containers
language: python
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  echo "travis ci connected"

sudo: required
